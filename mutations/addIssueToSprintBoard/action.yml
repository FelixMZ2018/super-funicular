name: move issue to sprint board
description: moves an issue to a sprintboard

inputs:
  PROJECT_NUMBER:
    description: 'Project Number'
  OWNER:
    description: the owner of the project
  GITHUB_TOKEN:
    description: the github token with the approriate access
runs:

  using: "composite"
  steps:
    - uses: actions/github-script@v6
      with:
        github-token: ${{ inputs.GITHUB_TOKEN }}
        script: |
          const getBoardQueryVariables = {
            owner: '${{inputs.OWNER}}',
            projectNumber: parseInt('${{inputs.PROJECT_NUMBER}}')
          }

          const getBoardQuery = `query($owner: String!, $projectNumber: Int!) {
            user(login: $owner) {
              projectV2(number: $projectNumber) {
                fields(last: 20) {
                  nodes {
                    ... on ProjectV2Field {
                      id
                      name
                    }
                    ... on ProjectV2IterationField {
                      id
                      name
                    }
                    ... on ProjectV2SingleSelectField {
                      id
                      name
                      options {
                        name
                        id
                      }
                    }
                  }
                }
              }
            }
          }`


          const getBoardQueryVariables = await github.graphql(getBoardQuery, getBoardQueryVariables)

          const sprintBoard = boardQueryREsult.data.viewer.projectV2.fields.nodes.find((node => node.name === getBoardQueryVariables.iterationFieldName))
          const iteration = sprintBoard.configuration.iterations[0]

          console.log({sprintBoard,iteration})

